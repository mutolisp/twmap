% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distrmap.R
\name{distrmap}
\alias{distrmap}
\title{Plot Species Distribution Map with Vertical Profile}
\usage{
distrmap(pts, boundary, col = "black", vert_prof, cex = 0.3, pch = 19)
}
\arguments{
\item{pts}{A data frame or list with three columns: x (longitude), y (latitude), and z (elevation).}

\item{boundary}{Character. File path or name of the boundary shapefile (to be loaded as an sf object).}

\item{col}{Character or vector of characters. Color(s) used for plotting the species points. Default is "black".}

\item{vert_prof}{A data frame or list containing the island's elevation profile data 
(typically generated by a function like `lat_elev_profile`).}

\item{cex}{Numeric. Magnification factor for plotting symbols. Default is 0.3.}

\item{pch}{Numeric. Plotting character used for points. Default is 19 (solid circle).}
}
\value{
The function is used for its side effect (plotting) and returns \code{NULL} invisibly.
}
\description{
This function generates a map displaying species distribution points (x, y) 
and their corresponding elevation profiles (y, z). It uses Base R graphics.
}
\examples{
\dontrun{
# Assuming the package name is 'twmap' and files are in inst/extdata
# Get the path for the boundary shapefile (LanyuBorder.shp)
boundary_path <- system.file("extdata", "LanyuBorder.shp", package = "twmap")

# Generate the elevation profile data using the GeoTIFF (LanyuDTM.tif)
# Note: lat_elev_profile is assumed to be an existing function in your package
profile_data <- lat_elev_profile(system.file("extdata", "LanyuDTM.tif", package = "twmap"))

# Load example species points data
data("sample_spts.ly") 

# Plot the map
distrmap(sample_spts.ly, 
         boundary = boundary_path, 
         vert_prof = profile_data, 
         col = "blue")
}

}
